[build]
builder = "nixpacks"
buildCommand = "pnpm install --frozen-lockfile && pnpm --filter @kiosk/server build"

[deploy]
startCommand = "node server/dist/index.js"
restartPolicyType = "on_failure"
