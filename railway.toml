[build]
builder = "nixpacks"
buildCommand = "pnpm install --frozen-lockfile && pnpm --filter @kiosk/server build"

[deploy]
startCommand = "NODE_OPTIONS='--env-file-if-exists=server/.env' node server/dist/index.js"
restartPolicyType = "on_failure"
