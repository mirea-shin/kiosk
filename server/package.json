{
  "name": "@kiosk/server",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "NODE_OPTIONS='--env-file-if-exists=../.env' tsx watch src/index.ts",
    "build": "tsc",
    "start": "NODE_OPTIONS='--env-file-if-exists=../.env' node dist/index.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "seed": "NODE_OPTIONS='--env-file-if-exists=../.env' tsx src/seed.ts",
    "seed:orders": "NODE_OPTIONS='--env-file-if-exists=../.env' tsx src/seed-orders.ts"
  },
  "dependencies": {
    "@hono/node-server": "^1",
    "@hono/node-ws": "^1.3.0",
    "@kiosk/shared": "workspace:*",
    "better-sqlite3": "^11",
    "hono": "^4"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7",
    "@types/node": "^22",
    "tsx": "^4",
    "vitest": "^3.2.4"
  }
}
